{
  "installCommand": "npm install",
  "buildCommand": "npm run build:packages && npm run build --workspace=@prestalink/web",
  "outputDirectory": "apps/web/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
